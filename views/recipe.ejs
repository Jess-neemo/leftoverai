<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipe Results</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>🍳 Recipe Results</h1>
        <p>Recipes for: <strong><%= ingredients %></strong></p>
        
        <div class="recipes">
            <% recipes.forEach(recipe => { %>
            <div class="recipe-card">
                <img src="<%= recipe.image %>" alt="<%= recipe.title %>">
                <h3><%= recipe.title %></h3>
                <p><strong>Used:</strong> <%= recipe.used %></p>
                <p><strong>Missing:</strong> <%= recipe.missed %></p>
                <div class="recipe-actions">
                    <a href="<%= recipe.link %>" target="_blank">View Recipe</a>
                    <a href="<%= recipe.save_url %>">Save to Favorites</a>
                </div>
            </div>
            <% }); %>
        </div>
        
        <a href="/" class="back-link">← Back to Search</a>
    </div>
</body>
</html>